DATABASE_URL="postgresql://postgres:[password]@[host]:5432/postgres"

# Leave unset. Set to "true" only for the short moment you intend to wipe every
# package, booking and customer row — the reset endpoints refuse to run without it.
# ALLOW_DATA_RESET="true"
